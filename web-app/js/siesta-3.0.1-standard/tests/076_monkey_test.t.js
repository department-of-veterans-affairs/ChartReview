StartTest(function (t) {

    t.it('Monkey test should handle a page redirect, when there is no BODY el for a while', function (t) {

        //t.monkeyTest(document.body, 10);

        //document.body.parentNode.removeChild(document.body);


    });
});
